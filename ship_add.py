def battleship_ship_add(ships, start_coordinates, orintation, ship_size, board_size):
    '''
    Ads ship to player at game start.
    Function return list with coordinates of all ship fields in format:
    ship = ["a1", "a2", "a3", "a4"]
    If error (out of board size or too close to other ship) return False
    '''
    ship = []
    
    return ship